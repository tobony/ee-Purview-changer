<Window x:Class="Ee.PurviewChanger.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ee Purview Changer"
        Height="860"
        Width="1180"
        MinHeight="720"
        MinWidth="1024">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Padding="16"
                    Margin="0,0,0,16"
                    Background="#FFF7E8"
                    BorderBrush="#FFD37A"
                    BorderThickness="1"
                    CornerRadius="8">
                <StackPanel>
                    <TextBlock FontSize="20" FontWeight="SemiBold" Text="Microsoft Purview 단건 라벨 변경 MVP" />
                    <TextBlock Margin="0,8,0,0"
                               TextWrapping="Wrap"
                               Text="첫 버전은 단건/검증 가능한 흐름에 집중합니다. 기본값은 검증 모드이며, 실제 로컬 파일 라벨 조회·변경은 향후 Microsoft Information Protection SDK 연동이 필요합니다." />
                    <TextBlock x:Name="ExecutionModeTextBlock"
                               Margin="0,8,0,0"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <GroupBox Grid.Row="1" Header="1. Microsoft 365 인증" Margin="0,0,0,16">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock x:Name="AuthenticationStatusTextBlock" FontWeight="SemiBold" />
                        <TextBlock x:Name="AuthenticationHintTextBlock" Margin="0,8,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                    <Button x:Name="SignInButton"
                            Grid.Column="1"
                            Margin="0,0,8,0"
                            Padding="14,8"
                            Content="SSO 로그인"
                            Click="SignInButton_OnClick" />
                    <Button x:Name="SignOutButton"
                            Grid.Column="2"
                            Padding="14,8"
                            Content="로그아웃"
                            Click="SignOutButton_OnClick" />
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="2" Header="2. 대상 파일 선택 및 현재 상태 확인" Margin="0,0,0,16">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="FilePathTextBox" Margin="0,0,8,0" VerticalContentAlignment="Center" />
                    <Button Grid.Column="1"
                            Margin="0,0,8,0"
                            Padding="14,8"
                            Content="찾아보기"
                            Click="BrowseButton_OnClick" />
                    <Button Grid.Column="2" Padding="14,8" Content="현재 상태 확인" Click="InspectButton_OnClick" />

                    <TextBlock Grid.Row="1"
                               Grid.ColumnSpan="3"
                               Margin="0,12,0,0"
                               FontWeight="SemiBold"
                               Text="검사 결과" />
                    <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Margin="0,8,0,0">
                        <TextBlock x:Name="InspectionSummaryTextBlock" TextWrapping="Wrap" />
                        <TextBlock x:Name="CapabilitySummaryTextBlock" Margin="0,4,0,0" TextWrapping="Wrap" />
                        <ItemsControl x:Name="InspectionMessagesItemsControl" Margin="0,8,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="3" Header="3. 대상 라벨 선택 및 변경 미리보기" Margin="0,0,0,16">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ComboBox x:Name="TargetLabelComboBox"
                              Margin="0,0,8,0"
                              DisplayMemberPath="Name"
                              SelectedValuePath="Id" />
                    <Button Grid.Column="1"
                            Margin="0,0,8,0"
                            Padding="14,8"
                            Content="미리보기"
                            Click="PreviewButton_OnClick" />
                    <Button x:Name="ApplyButton"
                            Grid.Column="2"
                            Padding="14,8"
                            Content="변경 적용"
                            Click="ApplyButton_OnClick" />

                    <TextBlock x:Name="PreviewSummaryTextBlock"
                               Grid.Row="1"
                               Grid.ColumnSpan="3"
                               Margin="0,12,0,0"
                               TextWrapping="Wrap" />
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="4" Header="4. 감사 로그" Margin="0,0,0,16">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="AuditLogTextBlock" VerticalAlignment="Center" TextWrapping="Wrap" />
                    <Button Grid.Column="1" Padding="14,8" Content="로그 폴더 열기" Click="OpenAuditFolderButton_OnClick" />
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="5" Header="5. Purview 기능 지원 현황">
                <DataGrid x:Name="CapabilitiesDataGrid"
                          Margin="12"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="220" Binding="{Binding Feature}" Header="기능" />
                        <DataGridTextColumn Width="220" Binding="{Binding Technology}" Header="API / SDK" />
                        <DataGridTextColumn Width="120" Binding="{Binding SupportLevel}" Header="상태" />
                        <DataGridTextColumn Width="*" Binding="{Binding Notes}" Header="설명" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </ScrollViewer>
</Window>
